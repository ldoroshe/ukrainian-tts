# GLOBAL_AGENTS placeholder (public fork safe)

This repository typically contains a `GLOBAL_AGENTS.md` symlink in private clones that points to the shared workspace global rules. Public forks should not expose private absolute paths. In this public fork, the symlink is replaced with this placeholder file. Contact the repository owner to obtain the workspace global rules location if needed.

<!-- End -->
